# Python
service/venv/
**/__pycache__/
*.pyc

# Node
**/node_modules/

# OS
.DS_Store
**/.DS_Store

# Build / scratch
build/
*.log
demo-export.svg

# Video sources: ignore everywhere EXCEPT the curated demo clips in assets/videos
*.mp4
*.mov
!assets/videos/*.mp4
!assets/videos/*.mov

# Stray symlinks to sibling/scratch dirs
comic
tests/node_modules

# MediaPipe pose venv (created by service/run-pose.sh)
mpvenv/

# VLM Router venv (created by service/run-router.sh)
routervenv/

# Local secrets (copy .env.example -> .env)
.env

# Vendored SEA-RAFT clone (opt-in engine; run service/setup-searaft.sh)
service/SEA-RAFT/

# Auto-downloaded model weights (YOLO etc.)
*.pt
